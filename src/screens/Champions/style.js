import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex:1,
        width:'100%',
    },
    listContainer: {
        marginLeft:'auto',
        marginRight:'auto',
        width:'95%',
        padding: 10,
        paddingBottom: 20,
    },
})

export default styles;
